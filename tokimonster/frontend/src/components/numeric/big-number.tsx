import {BNumberComponent as BNumber, Props} from '@shared/fe/component/big-number';

export const BNumberComponent:React.FC<Props> = props => {
    // To be customized in the future

    return <BNumber {...props}/>;
};
